---
# VPS Caddy Sites Configuration
# 
# Define all your reverse proxy sites here.
# Each site needs a domain pointing to the VPS public IP in DNS.

caddy_sites:
  # Example sites - customize for your setup
  - domain: passwords.pi.junttila.dev
    backend: "100.95.9.62:80"
  
  # - domain: app.pi.junttila.dev
  #   backend: "100.x.x.x:3000"
  
  # - domain: grafana.pi.junttila.dev  
  #   backend: "100.x.x.x:3001"
  
  # - domain: homeassistant.pi.junttila.dev
  #   backend: "100.x.x.x:8123"
